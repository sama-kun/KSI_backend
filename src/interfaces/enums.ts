export enum CartStatus {
  Warning = 'Warning',
  Complate = 'Complate',
  OnProject = 'OnProject',
}

export enum Role {
  USER = 'user',
  ADMIN = 'admin',
  ROOT = 'root',
}

export enum FileTypes {
  PDF = 'pdf',
  IMAGE = 'image',
}
